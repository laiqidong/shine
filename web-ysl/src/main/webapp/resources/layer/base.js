var themes_dir = "plugins/";
diyou.config({
    alias: {
        "validate": themes_dir + "jquery.validate",
        "submitform": themes_dir + "jquery.form",
        "layer": themes_dir + "layer/layer",
        "common": "app/common",
    }
});

